# dependencies
node_modules/

# build output
dist/
build/
apps/api/dist/
apps/web/dist/

# PWA build copied into the API static dir (keep the placeholder)
apps/api/src/static/*
!apps/api/src/static/.gitkeep

# env & secrets
.env
.env.local
.env.*.local
*.local

# prisma generated client lives in node_modules (already ignored)

# claude code: overrides locales (no compartir)
.claude/settings.local.json

# portadas subidas por usuarios — datos de runtime, fuera de git
Tapas/
apps/api/Tapas/

# logs & misc
*.log
npm-debug.log*
.DS_Store
coverage/
.vite/
