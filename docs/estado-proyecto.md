# Estado del proyecto — WMS Grupal (actualizado 2026-06-10)

> **Leer este archivo al iniciar una sesión de trabajo.** Resume qué está construido,
> cómo funciona el deploy y los errores ya resueltos para NO repetirlos.
> Las reglas de arquitectura viven en `CLAUDE.md` (raíz); esto es el estado operativo.
> **Performance / límites de Hostinger (procesos, queries, búsqueda): ver runbook
> `docs/performance-hostinger.md`** antes de tocar nada de eso.

## Qué está construido (Módulo 1 COMPLETO y en producción)

- **Esqueleto desplegable**: monorepo npm workspaces. Un solo proceso Node: NestJS sirve la API en `/api` y la PWA como estáticos.
- **Core**: auth JWT + RBAC granular, ABM de clientes / usuarios / roles+permisos / **transportistas** (2026-06-10), catálogo de productos (ISBN), auditoría inmutable, seed idempotente (los permisos NUEVOS del catálogo se asignan solos al rol Administrador existente; los demás roles via ABM).
- **Devoluciones**: máquina de estados completa (`A_APROBAR → … → PROCESADO`), control bulto por bulto, reconciliación por ISBN (con título), `UbicacionResolverPort` (seam), eventos `devolucion.procesada` (campo `correccion?`) y `devolucion.estado_cambiado`. **Cierre del módulo 2026-06-10:** transportista obligatorio para despachar (validado contra `core_transportista`); detalle/reconciliación verifican **propiedad** (un cliente NO ve devoluciones ajenas); ISBN no catalogado se rechaza también en el control; observaciones por etapa se ACUMULAN (no se pisan) y la diferencia de peso exige observación propia del cierre; `recibir` exige ≥1 bulto y el control ≥1 línea; **corrección post-Procesado** con permiso `devolucion.corregir` (solo Admin por defecto): reemplaza el control de un bulto, audita y re-emite el evento con `correccion=true`.
- **PWA**: marca Grupal, escáner (BarcodeDetector + ZXing + wedge USB; la cámara NO re-suma un código sostenido en cuadro — hay que retirarlo y volver a mostrarlo), **offline del control** (outbox en localStorage con resync; los controles que el server rechaza al resincronizar quedan VISIBLES como "rechazados" en el indicador del header, no se descartan en silencio), informes Recharts, nav por permisos, **grilla TanStack v8** reusable (`components/grilla/DataGrid.tsx`: orden por columna + mostrar/ocultar columnas persistido + filtro de texto opcional; usada en devoluciones, clientes y **catálogo**. El catálogo ahora busca **server-side** —NO baja todo el catálogo— ver hardening 2026-06-14) en la lista de devoluciones con KPIs clicables que filtran, selector de transportista al declarar (y "Despachar" ya NO sigue si el guardado falla), panel de corrección para Admin en Procesado.
- **Calidad**: tests jest 56/56 (`npm test`): seam + ISBN + **permisos por endpoint** (metadata RBAC) + **máquina de estados / propiedad / reglas de observación / reconciliación multi-bulto / circuito e2e del criterio / corrección** sobre un Prisma fake en memoria (`autorizacion.service.spec.ts`) + **claves manuales/generadas** (`claves.spec.ts`). OpenAPI (`/api/docs` + `npm run openapi` → `docs/openapi.yaml`), CSP + rate limiting (login 10/min → 429).
- **Docs**: specs de módulos en `docs/modulos/` (movidos de la raíz del repo); contratos en `docs/contratos/` (`ubicacion-resolver-port.md`, `eventos-devoluciones.md`).
- **Identidad visual (2026-06-15)**: colores y tipografía **reales** del sitio grupaldistribuidora.com.ar aplicados a la PWA. Paleta del kit Elementor: primary `#6EC1E4` (celeste), accent `#61CE70` (verde), secondary `#54595F`, text `#7A7A7A`, **navy `#334F98`** (azul profundo de marca → chrome/topbar con logo blanco). Tipografía: **Fira Sans** (cuerpo) + **Jost** (nav/títulos), cargadas en `index.html`. Tokens en `tailwind.config.ts` (mismos nombres de clase `brand-*`/`shell-*`, solo cambiaron valores: `shell-*` ahora es rampa navy `#16224A→#334F98`; los `-ink` se oscurecieron a `blue-ink #1E7BA8` / `green-ink #16803C` para **contraste AA** del texto/botones sobre blanco — los brillantes solo se usan como fills/bordes). `theme_color` PWA = `#16224A`. OJO: la receta de `global.css` del CLAUDE.md **da 404**; la fuente real es el kit Elementor (`uploads/elementor/css/post-5.css` + `custom-frontend.min.css`) y el `<link>` a Google Fonts del home.
- **Conector WooCommerce — portadas (2026-06-14)**: módulo **Integraciones** (`apps/api/src/integraciones/`, primer conector externo). Completa `producto.imagen_url` con la **URL de la imagen hosteada en WooCommerce** (no descarga: referencia la URL), matcheando **SKU = ISBN**. Lee `WOO_URL/WOO_KEY/WOO_SECRET` de env (Basic Auth a `/wp-json/wc/v3/products?sku=`); **inerte si faltan** (no rompe el arranque). Corre solo **cada 48 h** (`@Interval`, desde el arranque del proceso — reinicio/deploy reinicia el timer) + disparo manual `POST /api/integraciones/woocommerce/sync-imagenes` y `GET .../estado` (permiso `catalogo.administrar`). Solo procesa productos sin imagen (lote 200). CSP `img-src` suma el host de `WOO_URL` (si las portadas vinieran de otro dominio/CDN hay que agregarlo). Tests: `woocommerce.service.spec.ts` (núcleo `procesar`). NO se removió el upload manual ni `sharp` (siguen como alternativa).
- **Configuración del WMS (2026-06-14)**: nueva página admin `apps/web/src/pages/Configuracion.tsx` (ruta `/configuracion`, nav con permiso `catalogo.administrar`). Botón "Actualizar portadas ahora" que dispara el POST de WooCommerce con la sesión logueada y muestra el resumen (actualizadas/revisadas/sin imagen/errores) + estado del conector. `GET .../estado` ahora reporta `variables: {url,key,secret}` (solo presencia, sin valores) y la pantalla muestra cuál falta. El endpoint NO es navegable por GET (cae al catch-all de la SPA → login); ejecutar desde acá o por curl con token.
- **UX de carga de devoluciones (2026-06-14)**: 4 mejoras sobre el circuito de devolución, todas reusando piezas existentes y sin cruzar límites de módulo (catálogo se resuelve por ID/ISBN, sin FK):
  - **Doble click abre la devolución**: prop reusable `onRowDoubleClick` en `DataGrid` (con guardia `closest('button,a,input,select,label')` para no disparar navegación ni doble-ejecutar acciones al hacer doble click sobre un control de la fila). `Lista.tsx` navega a `/devoluciones/:id` con `useNavigate`. Cursor + tooltip "Doble click para abrir".
  - **Typeahead de producto con portada (desde 4º char)**: `Scanner` recibe prop opcional `onElegir`; al tipear ≥4 chars sugiere productos con miniatura/título/ISBN/editorial vía `lib/producto.ts buscarProductos` (GET `/catalogo/productos?q=`, debounce 250ms, `reqRef` descarta respuestas fuera de orden, click-away). Navegación ↑↓; **Enter** carga la sugerencia resaltada o la 1ª (evita resolver un título como ISBN). El escaneo cámara/USB-wedge sigue igual (resuelve por ISBN). Usado en declaración del cliente y control en depósito.
  - **Miniatura + popup de producto**: `ProductoThumb` (miniatura clickeable) + `ProductoDialog` (popup con portada grande, título, editorial, ISBN) en `ui.tsx`. Tipo común `ProductoLite` en `lib/producto.ts`.
  - **Backend**: `imagenUrl` agregado a `ProductoResuelto` (`resolverPorIsbn`/`Batch`) y al detalle de devolución — `titulosPorProducto`→`infoPorProducto` devuelve `{titulo,editorial,imagenUrl}` y `conTitulo` lo agrega a declaraciones/controles, así al **reabrir** una devolución ya cargada también se ven las portadas. Tests 57/57.
- **Hardening DB + seguridad (2026-06-14)** — auditoría con agentes en paralelo y fixes aplicados:
  - **SEGURIDAD**: `JWT_SECRET` ahora es **OBLIGATORIO en producción** — se eliminó el fallback `'dev-only-secret'`; si `NODE_ENV=production` y falta, el arranque FALLA (helper `core/auth/jwt-secret.ts`). En dev usa un secreto efímero con warning. **Clave admin**: sin `ADMIN_PASSWORD` el seed genera una **aleatoria** (la loguea una vez) en vez del literal `Admin1234!`. **CORS**: por defecto NO se habilita en prod (deployable único = same-origin); solo si se setea `CORS_ORIGIN` (lista por comas). En dev se permite todo.
  - **PERFORMANCE (importadores a batch)**: `catalogo.importarProductos` y `clientes.importar` pasaron de N+1 (1-3 queries por fila) a **batch**: 1 `findMany({in})` para clasificar + `createMany` para nuevos + updates agrupados en `$transaction` por bloques. `catalogo.bulkUpsert` con concurrencia acotada (5). Sync WooCommerce `procesar` ahora paraleliza **8 productos a la vez** (antes: HTTP secuencial por ISBN con timeout 15s → lote eterno).
  - **PAGINACIÓN + ÍNDICES**: `clientes.listar` pagina SERVER-SIDE (`{total, items}`, default 50 / máx 500, búsqueda `q` por nombre/nro con `contains`) desde 2026-06-14 — antes capaba a 2000 y filtraba client-side, así que un cliente más allá del tope no aparecía ni buscándolo (mismo bug de doble paginación que el catálogo). `usuarios.listar` (1000), `auditoria.listarPorEntidad` (200 últimos). Migración `20260614160000_indices_busqueda`: `@@index([nombre])` en `core_usuario` y `core_cliente` (evita filesort del orderBy), `@@index([created_at])` en `dev_autorizacion` (informe de serie). Las búsquedas `contains` (`%q%`) siguen sin usar índice (leading wildcard) — si el padrón crece mucho, evaluar FULLTEXT o `startsWith`.
  - **BÚSQUEDA DEL CATÁLOGO (server-side)**: antes el front bajaba TODO el catálogo (paginando de a 500) y filtraba client-side → lento y el parámetro `q` del backend no se usaba. Ahora: `producto.titulo` tiene **FULLTEXT** (`@@fulltext`, MATCH AGAINST boolean-mode con prefijo `palabra*`, fallback a `contains` para términos <3 chars por `innodb_ft_min_token_size`) + B-tree `@@index([titulo])` para el orderBy; ISBN y código por **prefijo** (`startsWith`, sargable sobre sus `@unique`). El front `Catalogo.tsx` busca server-side con debounce 300ms + paginación real (50/pág). Migración `20260614180000_producto_busqueda`. FULLTEXT funciona en Prisma 6.19.3 SIN preview features (verificado). Decisión: se perdió la búsqueda por **editorial** (no se filtraba server-side antes tampoco); el título busca por palabra interna.
  - **2ª tanda de fixes (2026-06-14, post-review con agentes)**: (a) **doble paginación del catálogo** — `DataGrid` paginaba client-side (20) las 50 filas que `Catalogo.tsx` ya paginaba server-side; se agregó prop `paginar` al DataGrid (default true) y `Catalogo` pasa `paginar={false}`. (b) **WooCommerce "solo 200"** — `productosSinImagen({limite, desdeId})` cursorea por id y `sincronizarImagenes` loopea hasta agotar TODO el catálogo (antes los irresolubles quedaban `null` al frente y se atascaba); guard de reentrancia compartido cron+manual (`enCurso`). (c) **N+1 en devoluciones** — `declarar`/`resolverControles` usan `resolverPorIsbnBatch` (1 query). (d) `autorizacion.listar` con `take: 1000`. (e) `roles.listar` con una sola query+include. (f) `escaparLike` en `core/util/bloques.ts` aplicado a clientes/usuarios. Tests 57/57.
  - **Picos de procesos en Hostinger (PENDIENTE de tu lado, ver `docs/performance-hostinger.md`)**: el grueso es CONFIG, no código — verificar `connection_limit` + `UV_THREADPOOL_SIZE` + `VIPS_CONCURRENCY` + `NODE_OPTIONS=--v8-pool-size=2` ACTIVAS en hPanel; topear workers de Passenger vía `.htaccess`; evaluar `engineType="library"` y mover migración al build. Config de hPanel: preset Other, Node 22.x, build `npm run build`, entry `apps/api/dist/main.js`.
  - Tests 56/56 (se actualizó el fake Prisma de `catalogo.import.spec.ts` al patrón batch). **Lo que YA estaba bien** (no se tocó): scrypt+lockout, throttler, helmet+CSP, ValidationPipe whitelist, upload por magic-bytes sin path traversal, `$queryRaw` solo estático, sin exposición de hashes, auditoría sin update/delete.
- **Integración de catálogo (2026-06-14)**: **import masivo** `POST /api/catalogo/productos/import` (catálogo simplificado **solo ISBN + Título + Editorial**; upsert por **ISBN** = identidad; el **ISBN actúa como código interno** — `codigoInterno` ahora es opcional en `ProductoDto` y se deriva del primer ISBN válido; máx 1000, idempotente; ISBN inválido no aborta el lote, se reporta en `errores`). Permiso `catalogo.administrar`. Manual para el sistema externo: `docs/integraciones/manual-api-catalogo.md`. Front: form de Catálogo simplificado a ISBN/Título/Editorial (se quitó código interno/autor/unidades de la pantalla). Tests: `catalogo.import.spec.ts` (upsert por ISBN, idempotencia, ISBN inválido). NO se tocó el modelo (decisión cerrada: código interno sigue siendo el ID maestro en DB).
  - **Portada de productos (imagen) 2026-06-14**: `core_producto.imagen_url`. `POST /api/catalogo/productos/:id/imagen` (multipart campo `imagen`, máx 8MB, permiso `catalogo.administrar`) **valida por magic bytes** (no por mimetype), **comprime a WebP** con `sharp` (resize ancho 600, q80) vía **import dinámico + fallback**: si sharp no carga, guarda el original sin romper. `DELETE` para quitarla. Archivos en carpeta **`Tapas`** ubicada **a la misma altura que `public_html`** (en Hostinger `/home/uXXXX/domains/<dominio>/Tapas`; se resuelve buscando `public_html` hacia arriba desde el cwd, override con `UPLOADS_DIR`), **fuera de dist/**; servidos como estático bajo **`/uploads`** (ServeStatic antes de la SPA); en dev Vite proxea `/uploads`→:3000. Front: columna "Portada" en la grilla de Catálogo con miniatura + subida inline (`api.upload` multipart). **Footgun Hostinger**: la persistencia de `uploads/` entre deploys NO está garantizada — setear `UPLOADS_DIR` a ruta absoluta persistente + backup (no hay object storage en plan gestionado). `sharp` usa binarios prebuilt `@img/*` (como Prisma), no compila → OK en Hostinger; NO usar `--omit=optional` en el build.
- **Integración de clientes (2026-06-10)**: `core_cliente` tiene `direccion`; **import masivo** `POST /api/clientes/import` (upsert por `nro_cliente`, máx 1000, idempotente, NO toca claves; importados quedan sin clave de portal hasta reset) + **autocomplete** `GET /api/clientes/buscar?q=` (número o nombre, máx 10, bloqueado para actores tipo cliente). Manual para el sistema externo: `docs/integraciones/manual-api-clientes.md` (auth = usuario dedicado `integrador` con permiso `cliente.administrar`). Front: `ClientePicker` en el form de nueva devolución. **Clientes inactivos** (`activo=false`, toggle en ABM o vía import): no aparecen en autocomplete, no pueden loguear ni crear devoluciones (las existentes se conservan como histórico); el ABM sí los lista para poder reactivarlos.

- **UX de cierre 2026-06-10 (segunda tanda)**:
  - **Claves elegibles**: en alta y reset de **clientes y usuarios**, el admin puede escribir la clave (mín. 8; queda definitiva, `primerIngreso=false`) o dejarla vacía → se genera aleatoria de 10 y se fuerza cambio al primer ingreso. Campo opcional `clave` en `CrearClienteDto`/`CrearUsuarioDto` + body `ResetClaveDto` en `POST .../:id/reset-clave`. Front: campo "Clave (opcional)" en el alta + `ClaveDialog` (components/ui.tsx) en el botón "Clave".
  - **Aprobación visible**: botón "Aprobar" directo en la grilla de devoluciones (solo con `solicitud.aprobar`) + en el detalle, aviso "Esperando aprobación" para quien no puede aprobar (explica quién aprueba y que los permisos se actualizan re-logueando, porque viajan en el JWT).
  - **Ayuda contextual ("?")**: botón en el header (AppShell) → panel lateral con el **manual de la pantalla actual**, escrito para no técnicos. Contenido en `apps/web/src/lib/ayuda.ts` (por ruta, secciones filtradas por tipo de actor: cliente no ve la operatoria interna); panel `apps/web/src/components/AyudaPanel.tsx` (ESC, scrim, aria-modal, full-screen en móvil).

## ⚠️ INCIDENTE ABIERTO (2026-06-10): producción 503
El dominio devuelve **503 de LiteSpeed** (el proceso Node no corre); build OK en deploy log. Fix preventivo ya aplicado (listen primero, migración en background). **Falta que el usuario revise**: botón Restart (badge "Running" del dashboard), y los **Runtime logs** (menú del dashboard ≠ log de deploy; o File Manager → `domains/<dominio>/nodejs/stdout.log` / `stderr.log`). El log de build que termina tras `nest build` es NORMAL. Investigación completa hecha con orquestador (causas rankeadas: crash al arranque por env/módulos vs proceso colgado de plataforma → Restart).

## Hosting / Deploy (Hostinger)

| Ítem | Valor |
|---|---|
| Dominio | `https://devoluciones.grupaldistribuidora.com.ar` |
| Tipo | Hostinger Business/Cloud, app Node.js gestionada (contenedor) |
| Repo | `github.com/tvivot/WMS`, rama `main`, **auto-deploy en push** |
| hPanel → Framework | Other · Node **22.x** · root `./` |
| hPanel → Build command | `npm run build` |
| hPanel → Entry file | `apps/api/dist/main.js` |
| hPanel → Output dir | **vacío** (la app Node sirve todo) |
| Health | `GET /api/health` → `{"status":"ok","db":"up"}` |
| Docs API | `/api/docs` (Swagger) |

### Base de datos (CRÍTICO — costó 4 iteraciones)
- MySQL de hPanel: base `u722074339_WMSGrupal`, usuario `u722074339_GruWMS`.
- **El host NO es `localhost`**: la app corre en un contenedor; `localhost:3306` responde pero es OTRO MySQL → "Authentication failed" con credenciales correctas.
- **Host correcto: `srv1894.hstgr.io`** + **Remote MySQL con "Cualquier host" (%)** habilitado en hPanel (sin el `%` rechaza al contenedor).
- `DATABASE_URL` lleva sufijo `?connect_timeout=15` (sin él, un host inalcanzable cuelga el health >60s).
- **Las env vars solo se leen al arrancar**: cambiar una variable exige redeploy/restart.

### Env vars en hPanel (nunca en el repo)
`DATABASE_URL`, `JWT_SECRET`, `ADMIN_PASSWORD` (y opcional `ADMIN_USERNAME`, `PORT`, `CORS_ORIGIN`).

## Decisiones técnicas con su porqué (no revertir sin motivo)

1. **Prisma `engineType = "binary"`** (schema.prisma): el engine por defecto (library/Node-API) **panickea** en Hostinger con `PANIC: timer has gone away`. No volver a library.
2. **Hash de claves con `scrypt` nativo de Node** (`core/seguridad/password.service.ts`): se evitó argon2/bcrypt porque son módulos nativos compilados (frágiles en Hostinger). La interfaz permite cambiarlo a futuro.
3. **Migraciones automáticas al arranque** (`src/migrate.ts` → `prisma migrate deploy`): el usuario NO tiene acceso a terminal en Hostinger. Idempotente; si falla, la app igual arranca y `/api/health` reporta `db:down`.
4. **Seed idempotente** (`core/seed/seed.service.ts`): crea permisos/roles/depósito/admin solo si faltan. **No pisa los permisos editados por el ABM de roles** (solo aplica defaults al CREAR el rol).
5. **`tsc` sin `incremental`** (apps/api/tsconfig.json): un `.tsbuildinfo` viejo + `deleteOutDir` de nest hacía que `main.js` no se emitiera. No reactivar.
6. **La API no importa `@wms/shared`**: en el build de Hostinger el symlink del workspace no resuelve (TS2307). Los tipos que necesita la API se definen localmente; `@wms/shared` lo usa la web.
7. **PWA**: `base:'/'` en vite.config es crítico. El build se copia con `scripts/copy-web.mjs` a `apps/api/src/static` ANTES de compilar la API (nest-cli lo lleva a `dist/static`).
8. **CSP en helmet** (main.ts): compatible con Google Fonts/cámara/blob; `/api/docs` queda excluido (Swagger usa inline scripts).

## Flujo de trabajo con el entorno local

- **Hooks del entorno bloquean** `git add/commit/push`, `docker rm` y lectura de archivos sensibles (`.env`, `.git-credentials`). **El usuario ejecuta los push** (se le pasa el bloque de comandos listo). No intentar saltear hooks.
- Verificación local: contenedor Docker `wms-mysql-skel` (MySQL 8, puerto **3307**, db `wms`, user `wms`/`wmspass`); `.env` local apunta ahí. `docker start wms-mysql-skel` si está detenido.
- Git: dos cuentas de GitHub en la máquina. El remote es `https://tvivot@github.com/tvivot/WMS.git` (fuerza usuario `tvivot`); hubo conflicto con credenciales cacheadas de `shopyglobalarg-bit`.
- Para correr el server local: `node apps/api/dist/main.js` con el `.env` de la raíz (no pasar `JWT_SECRET` inline: hook lo bloquea).

## Criterio de validación (pasó completo)
Vendedor crea → aprueba → cliente declara 3 ISBN/2 bultos/10kg → despacha → depósito recibe 2 bultos (6+4=10) → ingresa `DEV-01` → controla bulto por bulto (1 mal estado) → Procesado con reconciliación por ISBN correcta + auditoría completa + evento emitido. Test del seam: adapter falso de Ubicaciones sin tocar Devoluciones ✅.

## Notificaciones por email (core, transversal) — 2026-06-25

Módulo `core/notificaciones`: envía mails en cada **cambio de estado** de una devolución, con destinatarios **configurables por estado**.

- **Cómo se engancha:** suscriptor (`@OnEvent('devolucion.estado_cambiado')`) del evento de dominio que ya emite Devoluciones. **No toca Devoluciones** salvo enriquecer el evento con `clienteId` (contrato en `docs/contratos/eventos.md`). Respeta el límite de módulo: se suscribe por nombre de evento, no importa internos.
- **Config (ABM en `/notificaciones`, permiso `notificaciones.administrar`):**
  - **Grupos de correo** (`core_grupo_correo`): nombre + lista de emails (coma/;/salto).
  - **Reglas por estado** (`core_notificacion_regla`, una por `modulo+estado`): apunta a **varios grupos y/o usuarios internos** (N:N) + flag **incluir cliente** + plantilla **asunto/cuerpo** editable con placeholders `{{nro}} {{cliente}} {{estado}} {{estadoAnterior}} {{fecha}}`. Las 6 reglas vienen precargadas **desactivadas**.
- **Cliente:** se agregó `core_cliente.email` (varios separados por coma), editable en el ABM de clientes.
- **Office365:** envío por **Microsoft Graph** (app-only, client credentials), **sin dependencias npm** (fetch nativo + token cacheado, reintenta 1 vez ante 401). Env vars: `O365_TENANT_ID/O365_CLIENT_ID/O365_CLIENT_SECRET/MAIL_FROM`. Si faltan → envío deshabilitado, la UI lo avisa, los avisos quedan en cola. **Requiere registrar app en Azure AD con permiso de aplicación `Mail.Send` + consentimiento de admin.**
- **Outbox/reintento:** `core_notificacion_log` registra cada envío (PENDIENTE/ENVIADO/ERROR, cuerpo renderizado). Cron cada 5 min reintenta ERROR/PENDIENTE con `intentos<5`, con **guarda anti-solape** y **ventana de 2 min** (no pisa el envío inline en vuelo → evita duplicados).
- **Migración:** `20260625120000_core_notificaciones`. **Pendiente de deploy** (corre con `prisma migrate deploy`).

## Reformulación de control + reconciliación por lote del ERP — 2026-06-30

Cambio de fondo en el cierre de devoluciones (decidido con el usuario):

- **El conteo de libros por ISBN salió del control.** El **control** ahora es: por bulto, **registrar el peso** y marcarlo controlado (`POST .../bultos/:n/control` body `{peso}`). El conteo físico de libros se hace en **otro proceso** (a definir).
- **Lote del ERP (Fierro)** importado por el integrador: tablas `dev_lote` (codigo UNIQUE = `return_lot.document_id`) + `dev_lote_item`. Entra por **`POST /api/integraciones/devoluciones/lotes/import`** (permiso `devolucion.importar`) vía `DevolucionesLotePort` (Integraciones no toca internos). Upsert por codigo, reemplaza renglones. Contrato: [docs/contratos/devoluciones-lote-port.md].
- **Cierre exige `loteCodigo`** (nueva columna en `dev_autorizacion`): valida que el lote exista y sea del mismo cliente. La **reconciliación** compara, por ISBN, **lo declarado (WMS) vs la cantidad del lote (Fierro)** → `diferencia` (faltante/sobrante). Reemplaza la vieja "declarado vs recibido (control) + exceso de consignación".
- **`devolucion.procesada`**: `ReconciliacionLinea` cambió a `declarado/cantidadFierro/diferencia` (antes `recibido/bueno/malo/consignación`). Contrato en [docs/contratos/eventos.md]. Lo consumirá Inventario.
- **El gate de consignación sigue en `declarar()`** (saldo + excepciones); se quitó solo el chequeo de exceso del cierre (era redundante).
- **Chequeo periódico (cron 15 min):** `LoteScheduler` → `evaluarLotesPendientes()` compara, por cada devolución declarada-y-sin-procesar con lote asignado, declarado vs lote del ERP, y emite `devolucion.lote_evaluado` **solo si cambió** (dedup por `lote_validacion_firma`). El lote se asigna antes del cierre: `PATCH /devoluciones/autorizaciones/:id/lote` (permiso `deposito.ingresar`) + `PanelLote` en la UI. El mail a los responsables lo manda Notificaciones con una regla por estado lógico **LOTE_EVALUADO** (seedeada desactivada, placeholder `{{detalle}}`).
- **Migraciones pendientes de deploy:** `20260625130000_dev_lote`, `20260625140000_dev_autorizacion_lote`, `20260630120000_dev_lote_validacion`.
- **Follow-up:** el KPI de informes "calidad de libros (bueno/malo)" leía `dev_control`; deja de poblarse desde acá (va al otro proceso). No rompe; reformular cuando exista ese proceso.

## Próximos pasos (roadmap CLAUDE.md)
1. **Ubicaciones** (`ubi_*`): al conectarlo, cambiar UNA línea en `devoluciones.module.ts` (`provide: UBICACION_RESOLVER`).
2. **Integraciones** (`int_*`) antes del primer sync externo.
3. **Inventario** (`inv_*`): consume `devolucion.procesada` (ya se emite).
4. Pendientes menores: code-split del bundle web (~666KB por ZXing/Recharts), 2FA (diseño preparado, no implementar aún).
